document.addEventListener('DOMContentLoaded',function(){initBlackHoleCanvas();initFAQSystem();initSearchFunctionality();/*initCosmicInteractions();*/initMobileNavigation();initParallaxEffects();console.log('SRAC FAQ: Cosmic black hole environment activated');});function initBlackHoleCanvas(){const canvas=document.getElementById('blackhole-canvas');const ctx=canvas.getContext('2d');let animationId;let time=0;const particles=[];const mouse={x:window.innerWidth/2,y:window.innerHeight/2};function resizeCanvas(){canvas.width=window.innerWidth;canvas.height=window.innerHeight}function createParticles(){for(let i=0;i<100;i++){particles.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,vx:(Math.random()-0.5)*0.5,vy:(Math.random()-0.5)*0.5,size:Math.random()*2+0.5,opacity:Math.random()*0.8+0.2,color:`hsl(${250+Math.random()*60},80%,${60+Math.random()*40}%)`,angle:Math.random()*Math.PI*2,distance:Math.random()*200+50})}}function drawBlackHole(){const centerX=canvas.width/2;const centerY=canvas.height/2;const eventHorizonRadius=60;const accretionRadius=120;ctx.save();const eventHorizon=ctx.createRadialGradient(centerX,centerY,0,centerX,centerY,eventHorizonRadius);eventHorizon.addColorStop(0,'rgba(0,0,0,1)');eventHorizon.addColorStop(0.7,'rgba(168,85,247,0.8)');eventHorizon.addColorStop(1,'rgba(192,132,252,0.3)');ctx.fillStyle=eventHorizon;ctx.beginPath();ctx.arc(centerX,centerY,eventHorizonRadius,0,Math.PI*2);ctx.fill();const accretionGrad=ctx.createRadialGradient(centerX,centerY,eventHorizonRadius,centerX,centerY,accretionRadius);accretionGrad.addColorStop(0,'rgba(168,85,247,0.6)');accretionGrad.addColorStop(0.5,'rgba(192,132,252,0.4)');accretionGrad.addColorStop(1,'transparent');ctx.fillStyle=accretionGrad;ctx.save();ctx.translate(centerX,centerY);ctx.rotate(time*0.001);ctx.scale(1,0.3);ctx.beginPath();ctx.arc(0,0,accretionRadius,0,Math.PI*2);ctx.fill();ctx.restore();const jetGrad=ctx.createLinearGradient(centerX,centerY-200,centerX,centerY+200);jetGrad.addColorStop(0,'rgba(168,85,247,0.6)');jetGrad.addColorStop(0.5,'rgba(192,132,252,0.3)');jetGrad.addColorStop(1,'rgba(168,85,247,0.6)');ctx.fillStyle=jetGrad;ctx.fillRect(centerX-3,centerY-200,6,400);ctx.restore()}function updateParticles(){const centerX=canvas.width/2;const centerY=canvas.height/2;particles.forEach(particle=>{const dx=centerX-particle.x;const dy=centerY-particle.y;const distance=Math.sqrt(dx*dx+dy*dy);const force=500/Math.max(distance,50);particle.vx+=dx/distance*force*0.0001;particle.vy+=dy/distance*force*0.0001;particle.x+=particle.vx;particle.y+=particle.vy;if(distance<60){particle.x=Math.random()*canvas.width;particle.y=Math.random()*canvas.height;particle.vx=(Math.random()-0.5)*0.5;particle.vy=(Math.random()-0.5)*0.5}if(particle.x<0||particle.x>canvas.width||particle.y<0||particle.y>canvas.height){particle.x=Math.random()*canvas.width;particle.y=Math.random()*canvas.height;particle.vx=(Math.random()-0.5)*0.5;particle.vy=(Math.random()-0.5)*0.5}})}function drawParticles(){particles.forEach(particle=>{ctx.save();ctx.globalAlpha=particle.opacity;ctx.fillStyle=particle.color;ctx.beginPath();ctx.arc(particle.x,particle.y,particle.size,0,Math.PI*2);ctx.fill();ctx.shadowBlur=10;ctx.shadowColor=particle.color;ctx.restore()})}function animate(){ctx.clearRect(0,0,canvas.width,canvas.height);time+=16;drawBlackHole();updateParticles();drawParticles();animationId=requestAnimationFrame(animate)}resizeCanvas();createParticles();animate();window.addEventListener('resize',resizeCanvas);document.addEventListener('mousemove',e=>{mouse.x=e.clientX;mouse.y=e.clientY})}function initFAQSystem(){const navItems=document.querySelectorAll('.faq-nav-item');const categories=document.querySelectorAll('.faq-category');const faqItems=document.querySelectorAll('.faq-item');navItems.forEach(item=>{item.addEventListener('click',function(){const category=this.dataset.category;navItems.forEach(nav=>nav.classList.remove('active'));this.classList.add('active');categories.forEach(cat=>{cat.classList.remove('active');if(cat.id===category){setTimeout(()=>cat.classList.add('active'),100)}});document.querySelector('.faq-content').scrollIntoView({behavior:'smooth',block:'start'})})});faqItems.forEach(item=>{const question=item.querySelector('.faq-question');question.addEventListener('click',function(){const isActive=item.classList.contains('active');faqItems.forEach(otherItem=>{if(otherItem!==item)otherItem.classList.remove('active')});if(!isActive){item.classList.add('active');item.style.transform='scale(1.02)';setTimeout(()=>item.style.transform='scale(1)',200)}else{item.classList.remove('active')}})});categories.forEach(category=>{const firstItem=category.querySelector('.faq-item');if(firstItem)firstItem.classList.add('active')})}function initSearchFunctionality(){const searchInput=document.getElementById('faq-search');const searchResults=document.getElementById('search-results');const allItems=document.querySelectorAll('.faq-item');let totalVisible=allItems.length;searchInput.addEventListener('input',function(){const searchTerm=this.value.toLowerCase().trim();totalVisible=0;allItems.forEach(item=>{const searchData=item.dataset.search||'';const questionText=item.querySelector('h3').textContent.toLowerCase();const answerText=item.querySelector('.faq-answer p').textContent.toLowerCase();const isVisible=searchTerm===''||searchData.includes(searchTerm)||questionText.includes(searchTerm)||answerText.includes(searchTerm);if(isVisible){item.style.display='block';item.style.opacity='1';item.style.transform='translateY(0)';totalVisible++}else{item.style.opacity='0';item.style.transform='translateY(-10px)';setTimeout(()=>item.style.display='none',200)}});searchResults.textContent=searchTerm?`${totalVisible} results found`:`${allItems.length} questions available`;if(searchTerm){const categories=document.querySelectorAll('.faq-category');categories.forEach(cat=>{const visibleItems=cat.querySelectorAll('.faq-item[style*="display: block"], .faq-item:not([style*="display: none"])');if(visibleItems.length>0){cat.classList.add('active')}else{cat.classList.remove('active')}})}});searchInput.addEventListener('focus',function(){this.parentElement.querySelector('.search-glow').style.opacity='1'});searchInput.addEventListener('blur',function(){this.parentElement.querySelector('.search-glow').style.opacity='0'})}function createCosmicRipple(x,y){const ripple=document.createElement('div');ripple.style.cssText=`position:fixed;left:${x-10}px;top:${y-10}px;width:20px;height:20px;border:2px solid rgba(168,85,247,0.6);border-radius:50%;pointer-events:none;z-index:9999;animation:cosmicRipple 1.5s ease-out forwards;`;document.body.appendChild(ripple);setTimeout(()=>ripple.remove(),1500)}function initMobileNavigation(){const hamburger=document.querySelector('.hamburger-modern');const navMenu=document.querySelector('.nav-menu-modern');if(hamburger&&navMenu){hamburger.addEventListener('click',()=>{navMenu.classList.toggle('active');hamburger.classList.toggle('active');document.body.style.overflow=navMenu.classList.contains('active')?'hidden':'auto'})}}function initParallaxEffects(){let ticking=false;function updateParallax(){const scrolled=window.pageYOffset;const rate=scrolled*-0.5;const parallaxElements=document.querySelectorAll('.cosmic-overlay, .particle-field');parallaxElements.forEach(el=>{el.style.transform=`translate3d(0, ${rate}px, 0)`});ticking=false}function requestTick(){if(!ticking){requestAnimationFrame(updateParallax);ticking=true}}window.addEventListener('scroll',requestTick)}const cosmicCSS=`@keyframes cosmicRipple{0%{transform:scale(0);opacity:1}100%{transform:scale(10);opacity:0}}@keyframes pulseGlow{0%,100%{opacity:0.5;transform:scale(1)}50%{opacity:1;transform:scale(1.05)}}@keyframes floatUpDown{0%,100%{transform:translateY(0)}50%{transform:translateY(-10px)}}`;const cosmicStyle=document.createElement('style');cosmicStyle.textContent=cosmicCSS;document.head.appendChild(cosmicStyle);
